#ifndef INTS_H
#define INTS_H

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

#define INTS 30

void printIntArray(const int *ints);

int compareTwoInts(const void *a, const void *b);

#endif // INTS_H
